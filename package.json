{
  "name": "@toolchain/node",
  "version": "0.1.0",
  "description": "A Docker toolchain for node apps",
  "main": "index.js",
  "scripts": {
    "clean": "./scripts/clean.sh",
    "build": "./scripts/build.sh",
    "generate": "./scripts/generate.sh",
    "clean-generate": "npm run clean && npm run generate",
    "clean-build": "npm run clean-generate && npm run build",
    "release": "npm run clean-build && ./scripts/release.sh"
  },
  "author": "",
  "license": "ISC"
}
